spring:
  application:
    name: ledger-service

  datasource:
    url: jdbc:postgresql://localhost:5015/ledger_service_db
    username: "root"
    password: "0000"
    driver-class-name: org.postgresql.Driver

  # JPA configurations
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
